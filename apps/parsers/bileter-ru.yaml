apiVersion: batch/v1
kind: CronJob
metadata:
  name: parser-bileter-ru
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            environment: app
          tolerations:
          - key: app
            operator: Equal
            effect: NoSchedule
          containers:
          - name: bileter-ru
            image: lon8/bileter_ru:latest
            env:
            - name: SERVER_HOST
              value: "http://fastapi-service.notickets.svc.cluster.local"
            - name: SERVER_PORT
              value: "8000"
          restartPolicy: OnFailure